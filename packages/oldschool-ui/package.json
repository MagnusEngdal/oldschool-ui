{
  "private": false,
  "name": "oldschool-ui",
  "version": "1.1.2",
  "description": "Old-school component library for React",
  "repository": {
    "type": "git",
    "url": "https://github.com/MagnusEngdal/oldschool-ui.git"
  },
  "scripts": {
    "build": "tsc && esbuild --bundle src/index.tsx --format=esm --minify --sourcemap --outfile=dist/index.js",
    "watch": "tsc && esbuild --bundle src/index.tsx --format=esm --sourcemap --watch --outfile=dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "main": "dist/index.js",
  "author": "Magnus Engdal",
  "license": "ISC",
  "devDependencies": {
    "@types/react": "^17.0.37",
    "esbuild": "^0.13.15",
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
